# IMPLEMENTATION_PLAN

## Priority Work

- [ ] `DESIGN-001` CLI operator design doctrine and help smoke coverage

  Spec: `specs/300426-operator-design-runtime-contract.md`
  Why now: `auto super` treats design/runtime integrity as blocking, but the live product surface is terminal UX and the repo only gained durable root design doctrine in this pass; high-value help and doctor surfaces need runtime smoke proof so docs do not drift from command reality.
  Codebase evidence: `DESIGN.md` now defines the operator design system; `src/main.rs` owns command help; `src/doctor_command.rs` owns first-run capability output; `.github/workflows/ci.yml` currently covers only selected help smoke.
  Source of truth: `src/main.rs`, `src/doctor_command.rs`, `DESIGN.md`
  Runtime owner: `src/main.rs`, `src/doctor_command.rs`
  UI consumers: terminal help, `auto doctor` stdout, README command guide, CI smoke logs
  Generated artifacts: `.auto/logs/*` prompt logs and CI command output only; no generated UI binding
  Fixture boundary: production help and doctor output must be generated by the compiled binary, not fixture help text or copied README snippets.
  Retired surfaces: stale command-count prose and old required-tool wording that contradicts runtime doctor capability checks
  Owns: `DESIGN.md`, `src/main.rs`, `src/doctor_command.rs`, `.github/workflows/ci.yml`, `README.md`, `AGENTS.md`
  Integration touchpoints: CLI help text, doctor required/capability sections, CI installed-binary smoke, README onboarding
  Scope boundary: terminal/operator output and docs only; do not change model routing, credentials, scheduler behavior, or launch a web UI.
  Acceptance criteria: `auto doctor`, `auto design --help`, and `auto super --help` are covered by tests or CI smoke; README/AGENTS/tool language distinguishes no-model preflight from workflow-specific tools; root `DESIGN.md` remains the canonical design doctrine.
  Verification: `cargo test doctor_command::tests`; `cargo run --quiet -- doctor`; `cargo run --quiet -- design --help`; `cargo run --quiet -- super --help`; `rg -n "Operator Design System|auto design|auto super|Required tools|capabilities" DESIGN.md README.md AGENTS.md .github/workflows/ci.yml src/main.rs src/doctor_command.rs`
  Required tests: `doctor_command::tests::doctor_checks_expected_help_surfaces`, `doctor_command::tests::doctor_reports_missing_optional_tools_without_panicking`, add or update installed-binary help smoke coverage for `auto design --help` and `auto super --help`
  Contract generation: none -- no generated contract
  Cross-surface tests: compiled-binary help/doctor smoke plus README/AGENTS grep proof
  Review/closeout: reviewer checks runtime help output against README/AGENTS/DESIGN wording and confirms no fixture help text was used as proof.
  Completion artifacts: `DESIGN.md`, `.github/workflows/ci.yml`, `README.md`, `AGENTS.md`
  Dependencies: none
  Estimated scope: S
  Completion signal: CLI help, doctor output, CI smoke, and root design doctrine agree on the operator surface.

- [ ] `DESIGN-002` Report-only and final-status output contract enforcement

  Spec: `specs/300426-operator-design-runtime-contract.md`
  Why now: The design system depends on operators knowing what each command read, wrote, proved, and should do next; today this is partly prompt discipline and partly command-specific behavior, so report-only and dry-run surfaces can still look safer or more complete than runtime enforcement proves.
  Codebase evidence: `src/qa_only_command.rs` has a dirty-state guard; `src/health_command.rs` writes prompt logs but does not verify `HEALTH.md`; `src/design_command.rs` verifies artifacts but lacks a general report-only dirty-state contract; `genesis/plans/010-audit-nemesis-and-report-only-lifecycle-truth.md` already flags this drift.
  Source of truth: `src/qa_only_command.rs`, `src/health_command.rs`, `src/design_command.rs`, `src/review_command.rs`
  Runtime owner: `src/qa_only_command.rs`, `src/health_command.rs`, `src/design_command.rs`
  UI consumers: terminal stdout summaries, `QA.md`, `HEALTH.md`, `.auto/design/*/DESIGN-REPORT.md`, prompt logs
  Generated artifacts: `QA.md`, `HEALTH.md`, `.auto/design/*`, `.auto/logs/*`
  Fixture boundary: tests may use fixture repos and stub Codex binaries, but production report success must be based on real files written by the command under test.
  Retired surfaces: prompt-only "report-only" claims with no runtime write-boundary check
  Owns: `src/qa_only_command.rs`, `src/health_command.rs`, `src/design_command.rs`, `src/review_command.rs`
  Integration touchpoints: report-only dirty-state checks, required report artifact verification, final status block labels, model stderr logs
  Scope boundary: enforce report/write boundaries and status labels; do not change the substantive QA, health, review, or design prompts beyond necessary contract wording.
  Acceptance criteria: report-only commands fail when workers change disallowed files; health/design/qa-only success requires non-empty expected reports; stdout names status, files written, blockers, and next step.
  Verification: `cargo test qa_only_command::tests`; `cargo test health_command::tests`; `cargo test design_command::tests::design_prompt_rejects_fake_mockup_and_requires_runtime_truth`; `rg -n "report-only|write boundary|HEALTH.md|QA.md|DESIGN-REPORT.md|status:" src/qa_only_command.rs src/health_command.rs src/design_command.rs src/review_command.rs`
  Required tests: existing `qa_only_command::tests`; add `health_requires_non_empty_report`, add `design_report_only_rejects_disallowed_dirty_state`, add output-label tests for final status blocks
  Contract generation: none -- no generated contract
  Cross-surface tests: command fixture tests prove stdout/report artifacts match allowed write sets
  Review/closeout: reviewer checks a failing fixture and a passing fixture for each touched report-only path.
  Completion artifacts: `src/qa_only_command.rs`, `src/health_command.rs`, `src/design_command.rs`, `src/review_command.rs`
  Dependencies: `DESIGN-001`
  Estimated scope: M
  Completion signal: report-only and dry-run surfaces no longer rely on prompt-only honesty.

- [ ] `DESIGN-003` Super, generation, and task contract parity

  Spec: `specs/300426-operator-design-runtime-contract.md`
  Why now: `auto gen` validates rich runtime/UI/generated/fixture fields, but `auto super` deterministic gating currently checks a smaller legacy field set; this can let design/runtime drift reach workers after the design gate has said GO.
  Codebase evidence: `src/generation.rs` defines `REQUIRED_PLAN_TASK_FIELDS` with source-of-truth and UI contract fields; `src/spec_command.rs` enforces similar fields for `auto spec`; `src/super_command.rs` `verify_super_task` only checks the older minimal list; `src/task_parser.rs` already parses shared task blocks.
  Source of truth: `src/generation.rs`, `src/spec_command.rs`, `src/super_command.rs`, `src/task_parser.rs`
  Runtime owner: `src/generation.rs`, `src/super_command.rs`
  UI consumers: generated `IMPLEMENTATION_PLAN.md`, `auto super` execution gate output, `auto parallel` worker prompts, review handoffs
  Generated artifacts: `gen-*/IMPLEMENTATION_PLAN.md`, root `IMPLEMENTATION_PLAN.md`, `.auto/super/*/DETERMINISTIC-GATE.json`
  Fixture boundary: schema tests may use fixture plan rows, but production queue acceptance must parse the live root plan.
  Retired surfaces: legacy active task shapes that omit source truth, runtime owner, UI consumers, generated artifacts, fixture boundary, contract generation, cross-surface tests, or review/closeout
  Owns: `src/generation.rs`, `src/spec_command.rs`, `src/super_command.rs`, `src/task_parser.rs`
  Integration touchpoints: generated plan validation, super deterministic gate, loop/parallel task selection, review plan writes
  Scope boundary: validation parity only; do not redesign the markdown task format or weaken `auto gen` validation.
  Acceptance criteria: the same malformed active task fails generation-style validation and super pre-parallel validation with row-specific error text; valid rich rows still pass; missing dependency and broad verification checks remain active.
  Verification: `cargo test generation::tests::generated_plan_rejects_missing_spec_refs`; `cargo test super_command::tests::deterministic_gate_rejects_package_wide_cargo_test`; `cargo test task_parser::tests::dependencies_none_and_multiline_notes_are_stable`; `rg -n "Source of truth:|Runtime owner:|UI consumers:|Contract generation:|Cross-surface tests:" src/generation.rs src/spec_command.rs src/super_command.rs src/task_parser.rs`
  Required tests: add `super_rejects_task_missing_runtime_ui_fields`, add `super_accepts_generated_rich_task_contract`, keep `generation::tests` validator coverage green
  Contract generation: none -- no generated contract
  Cross-surface tests: fixture plan row accepted by `auto gen` validation must also pass `auto super` deterministic gate; fixture row rejected by `auto gen` must be rejected before super parallel launch
  Review/closeout: reviewer checks the shared validation list has one owner or an explicit test that fails on drift.
  Completion artifacts: `src/generation.rs`, `src/spec_command.rs`, `src/super_command.rs`, `src/task_parser.rs`
  Dependencies: none
  Estimated scope: M
  Completion signal: super, gen, spec, and execution surfaces agree on what makes a queue row executable.

- [ ] `DESIGN-004` Design resolve NO-GO task preservation

  Spec: `specs/300426-operator-design-runtime-contract.md`
  Why now: A design resolve pass can produce actionable `DESIGN-PLAN-ITEMS.md` rows and still end NO-GO; if the final failing pass exits before preserving those rows in the executor queue, the design gate blocks without giving `auto parallel` durable work.
  Codebase evidence: `src/design_command.rs` promotes design plan items before intermediate implementation passes, but the final max-pass NO-GO path bails after the latest report; `genesis/plans/010-audit-nemesis-and-report-only-lifecycle-truth.md` calls out final-pass design NO-GO preservation.
  Source of truth: `src/design_command.rs`, `src/task_parser.rs`, root `IMPLEMENTATION_PLAN.md`
  Runtime owner: `src/design_command.rs`
  UI consumers: `.auto/design/*/DESIGN-REPORT.md`, `.auto/design/*/DESIGN-PLAN-ITEMS.md`, root `IMPLEMENTATION_PLAN.md`, `auto super` design status output
  Generated artifacts: `.auto/design/*/DESIGN-PLAN-ITEMS.md`, `.auto/design/*/DESIGN-RESOLVE-STATUS.md`, root `IMPLEMENTATION_PLAN.md`
  Fixture boundary: tests may use temporary design pass directories; production promotion must consume the actual pass directory produced by `auto design --resolve` or `auto super` design resolve.
  Retired surfaces: artifact-only design repair tasks that are not parser-visible to `auto parallel`
  Owns: `src/design_command.rs`, `src/task_parser.rs`
  Integration touchpoints: design artifact verifier, design plan block extraction, root queue insertion, resolve status writing, super design gate failure handling
  Scope boundary: preserve/promote unresolved design tasks; do not implement the tasks themselves inside the design audit pass.
  Acceptance criteria: on the final allowed resolve pass, a NO-GO report with unchecked design plan items promotes missing parser-visible tasks into root `IMPLEMENTATION_PLAN.md` or writes an explicit recovery artifact before returning failure; repeated promotion is idempotent.
  Verification: `cargo test design_command::tests::design_plan_items_promote_missing_executor_tasks_to_root_queue`; `cargo test task_parser::tests::parses_all_plan_statuses_and_fields`; `rg -n "promote_design_plan_items_to_root_queue|design resolve did not reach|DESIGN-PLAN-ITEMS.md" src/design_command.rs`
  Required tests: extend `design_plan_items_promote_missing_executor_tasks_to_root_queue`; add `final_no_go_promotes_design_tasks_before_failure`
  Contract generation: none -- no generated contract
  Cross-surface tests: fixture `DESIGN-PLAN-ITEMS.md` row appears in root plan and is parsed by `task_parser::parse_tasks`
  Review/closeout: reviewer runs the failing-pass fixture and confirms no actionable design task remains only in `.auto/design`.
  Completion artifacts: `src/design_command.rs`, `src/task_parser.rs`
  Dependencies: `DESIGN-003`
  Estimated scope: S
  Completion signal: design NO-GO always leaves executor-visible repair work or an explicit dependency blocker.

- [ ] `AD-014` Symphony and receipt evidence checkpoint

  Spec: `specs/230426-symphony-workflow-and-linear-sync.md`
  Why now: Workflow rendering and completion evidence both affect unattended execution safety; future Linear sync or external Symphony runtime changes should wait until these local deterministic proofs are recorded.
  Codebase evidence: `src/symphony_command.rs` already reconciles terminal Linear issues through `inspect_task_completion_evidence` before marking local tasks done; `src/completion_artifacts.rs` is the shared evidence gate; `scripts/run-task-verification.sh` is the receipt-producing wrapper.
  Owns: `REVIEW.md`
  Integration touchpoints: `src/symphony_command.rs`, `src/completion_artifacts.rs`, `scripts/run-task-verification.sh`, `.auto/symphony/verification-receipts`
  Scope boundary: Validation and handoff only; do not call Linear, render a live external Symphony workflow, or launch Symphony runtime.
  Acceptance criteria: `REVIEW.md` records hostile workflow render test outcomes, zero-test receipt outcomes, any intentionally untested live Linear or Symphony surfaces, and a go/no-go decision for adapter migration work.
  Verification: `cargo test symphony_command::tests::workflow_render_rejects_hostile_branch`; `cargo test symphony_command::tests::workflow_render_rejects_hostile_model_and_effort`; `cargo test completion_artifacts::tests::inspect_task_completion_evidence_rejects_zero_cargo_tests`; `cargo test completion_artifacts::tests::inspect_task_completion_evidence_requires_wrapper_for_executable_verification`; `rg -n "AD-014|Symphony|zero-test|receipt" REVIEW.md`
  Required tests: `symphony_command::tests::workflow_render_rejects_hostile_branch`, `symphony_command::tests::workflow_render_rejects_hostile_model_and_effort`, `completion_artifacts::tests::inspect_task_completion_evidence_rejects_zero_cargo_tests`, `completion_artifacts::tests::inspect_task_completion_evidence_requires_wrapper_for_executable_verification`
  Completion artifacts: `REVIEW.md`
  Dependencies: `AD-011`, `AD-013`
  Estimated scope: XS
  Completion signal: Review handoff records local green proof or specific blockers for workflow rendering and receipt evidence.

- [~] `TASK-016` Tag `v0.2.0` once the priority + first follow-on cluster is verified clean

    Spec: `specs/220426-release-ship.md`
    Why now: `Cargo.toml` is still on `0.1.0`; once the visible drift (README, CI, dead code, hardening) is closed, cutting a `0.2.0` annotated tag locks the verified baseline. Spec frames this as a preservation contract; the only new work here is the actual tag.
    Codebase evidence: `Cargo.toml:3` (`version = "0.1.0"`), `build.rs:8-62` provenance already wired, `src/util.rs:9-17` `CLI_LONG_VERSION`.
    Owns: `refs/tags/v0.2.0`
    Integration touchpoints: `Cargo.toml`, `Cargo.lock`, `COMPLETED.md`.
    Scope boundary: bump `Cargo.toml` version to `0.2.0`, regenerate `Cargo.lock`, append a `## v0.2.0` section to `COMPLETED.md` summarizing the closed task IDs, and create the annotated tag locally. Do NOT push the tag in this task — `auto ship` (or a separate operator step) handles publishing and PR plumbing per spec.
    Acceptance criteria: `Cargo.toml` reads `version = "0.2.0"`; `cargo build` regenerates `Cargo.lock` cleanly; `git tag -l v0.2.0` returns `v0.2.0`; the tag's annotation message lists TASK-001..TASK-011 plus any closed follow-ons; `auto --version` first line reads `auto 0.2.0`.
    Verification: `cargo build && ./target/debug/auto --version | head -1` (must read `auto 0.2.0`); `git tag -l v0.2.0` returns `v0.2.0`; `git cat-file -p v0.2.0` shows annotated message with task list.
    Required tests: none (release-mechanics only; `cargo test` regression already covered by prior checkpoints)
    Completion artifacts: `Cargo.toml`, `Cargo.lock`, `COMPLETED.md`, `refs/tags/v0.2.0`
    Dependencies: TASK-011
    Estimated scope: S
    Completion signal: annotated `v0.2.0` tag exists locally with the closed task list, `auto --version` confirms the bump.


## Follow-On Work
